{"responses":{"200":{"content":{"application/json":{"schema":{"type":"object"}}},"description":"Applied rect, monitor label, and target window"},"400":{"content":{"application/json":{"schema":{"description":"The error body every REST endpoint returns on `400`. Docs schema only:\n`respond()` builds this shape inline (pinned by the tests below), and the\nstruct exists so utoipa can describe it — hence the dead-code allowance.","properties":{"error":{"description":"Human-readable reason for the failure.","type":"string"}},"required":["error"],"type":"object","title":"ErrorBody"}}},"description":"Unknown preset, cells outside the grid, no target window or monitor match, or Accessibility not granted"}}}